name: CI
on:
  push:
    branches:
      - master
  pull_request:
  workflow_dispatch:

jobs:
  build:
    permissions:
      contents: write
    uses: status-im/nimbus-common-workflow/.github/workflows/common.yml@main
    with:
      nimble-bin-path: true
      test-command: |
        nim --version
        nimble --version
        nimble install -y --depsOnly
        nimble test
        env NIMFLAGS="--mm:refc" nimble test
        bash tests/scripts/tests.sh
